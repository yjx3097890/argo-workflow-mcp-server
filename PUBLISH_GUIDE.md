# npm 发布指南

## 发布前检查清单

✅ **所有测试通过**：20 个测试全部通过
✅ **package.json 配置正确**：包名、版本、依赖都已配置
✅ **包内容验证**：通过 `npm pack --dry-run` 检查，包含 67 个文件（32.9 kB）
✅ **LICENSE 文件存在**：MIT 许可证
✅ **README.md 完整**：包含安装、使用、配置说明
✅ **构建文件就绪**：dist 目录包含所有编译后的文件

## 发布步骤

### 1. 登录 npm

如果你还没有 npm 账号，先注册：https://www.npmjs.com/signup

然后登录：

```bash
npm login
```

输入你的 npm 用户名、密码和邮箱。

### 2. 检查包名是否可用

```bash
npm info argo-workflow-mcp-server
```

如果返回 404 错误，说明包名可用。如果已存在，需要更改包名。

### 3. （可选）更新 repository URL

如果你有 GitHub 仓库，更新 package.json 中的 repository URL：

```json
{
  "repository": {
    "type": "git",
    "url": "https://github.com/your-username/argo-workflow-mcp-server"
  }
}
```

### 4. 最后检查

```bash
# 确保所有测试通过
npm test

# 确保代码已构建
npm run build

# 再次检查包内容
npm pack --dry-run
```

### 5. 发布到 npm

```bash
npm publish
```

如果这是第一次发布，npm 会要求你验证邮箱。

### 6. 验证发布

```bash
# 查看包信息
npm info argo-workflow-mcp-server

# 测试安装
npx argo-workflow-mcp-server --help
```

## 发布后

### 创建 Git Tag

```bash
git tag v1.0.0
git push origin v1.0.0
```

### 更新版本号

后续发布时，使用以下命令更新版本号：

```bash
# 补丁版本（bug 修复）：1.0.0 -> 1.0.1
npm version patch

# 次版本（新功能）：1.0.0 -> 1.1.0
npm version minor

# 主版本（破坏性变更）：1.0.0 -> 2.0.0
npm version major
```

然后重新发布：

```bash
npm publish
```

## 故障排查

### 包名已存在

如果包名已被占用，修改 package.json 中的 `name` 字段，例如：
- `@your-username/argo-workflow-mcp-server`
- `argo-workflow-mcp`
- `argo-mcp-server`

### 权限错误

确保你已登录 npm：

```bash
npm whoami
```

如果未登录，运行 `npm login`。

### 邮箱未验证

npm 会发送验证邮件到你的注册邮箱，点击链接验证后再发布。

## 注意事项

1. **一旦发布，无法删除**：npm 包发布后 24 小时内可以撤销，之后只能废弃（deprecate）
2. **版本号不可重复使用**：每次发布必须使用新的版本号
3. **谨慎发布**：确保代码质量和文档完整性

## 当前包信息

- **包名**：argo-workflow-mcp-server
- **版本**：1.0.0
- **大小**：32.9 kB（压缩），168.4 kB（解压）
- **文件数**：67 个
- **许可证**：MIT
- **Node.js 要求**：>= 18.0.0

## 快速发布命令

如果一切准备就绪，执行以下命令：

```bash
# 1. 登录 npm
npm login

# 2. 发布
npm publish

# 3. 验证
npm info argo-workflow-mcp-server

# 4. 测试安装
npx argo-workflow-mcp-server --help
```

祝发布顺利！🚀
